# Application name
spring.application.name=spring-eb-app

# Elastic Beanstalk expects the app to listen on port 5000 by default
# (EB's nginx proxy forwards port 80 → 5000)
server.port=5000

# Log level
logging.level.root=INFO
logging.level.com.example=DEBUG
